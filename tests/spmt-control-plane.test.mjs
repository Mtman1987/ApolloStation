import assert from "node:assert/strict";
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import test from "node:test";
import { AuthorityService } from "../packages/authority-core/dist/index.js";
import { SqliteAuthorityStore } from "../packages/authority-sqlite/dist/index.js";
import { AuthService } from "../packages/auth-core/dist/index.js";
import { ControlService } from "../packages/control-core/dist/index.js";
import { PlatformOperations } from "../packages/platform-ops/dist/index.js";
import { PlatformApiAdapter } from "../packages/api-adapter/dist/index.js";
import { OutboxDispatcher } from "../packages/outbox-core/dist/index.js";
import { SpmtClient } from "../packages/sdk/dist/index.js";
import { runSpmtCli } from "../packages/cli/dist/index.js";
import { SpmtMcpServer, SPMT_MCP_PROTOCOL_VERSION } from "../packages/mcp/dist/index.js";

function env() {
  const dir = mkdtempSync(join(tmpdir(), "spmt-control-")); const path = join(dir, "authority.db"); const store = new SqliteAuthorityStore(path);
  let time = Date.parse("2026-08-22T03:00:00.000Z"); const now = () => new Date(time).toISOString(); const advance = (seconds) => { time += seconds * 1000; };
  const authority = new AuthorityService({ store, now }); authority.ensureUser("owner-1"); authority.getOrCreateWorkspace("tenant-a");
  const control = new ControlService({ store, now }); control.registerTenant({ tenantId: "tenant-a", ownerUserId: "owner-1", displayName: "Tenant A" }); control.registerApp({ appId: "space-mountain", name: "SpaceMountain", description: "Command Bridge", version: "1.0.0", launchUrl: "https://spacemountain.live/", allowedScopes: ["workspace:read"], surfaces: ["shell", "standalone", "overlay"], status: "active" });
  const auth = new AuthService({ store, now, tokenFactory: (() => { let n=0; return (kind) => `${kind}_${++n}_${"x".repeat(32)}`; })() }); auth.registerServiceIdentity({ serviceId: "reference-app", credential: "reference-app-control-secret-12345", scopes: ["apps:read","apps:install","entitlements:read","events:read","events:write"], tenantMode: "allow-list", tenantIds: ["tenant-a"] });
  const token = auth.issueServiceAccess("reference-app", "reference-app-control-secret-12345").accessToken;
  const operations = new PlatformOperations(auth, authority, control); const api = new PlatformApiAdapter(operations);
  return { dir, path, store, authority, control, auth, token, operations, api, advance };
}
function cleanup(e) { e.store.close(); rmSync(e.dir, { recursive: true, force: true }); }

test("app registry, install, entitlement and tenant isolation share one durable authority", () => { const e = env(); try { assert.equal(e.api.handle({ method:"GET", path:"/v1/apps", headers:{ authorization:`Bearer ${e.token}` } }).status, 200); const catalogWrite=e.api.handle({method:"POST",path:"/v1/apps",headers:{authorization:`Bearer ${e.token}`},body:{appId:"unauthorized-app",name:"Unauthorized",description:"Must not be registered",version:"1.0.0",launchUrl:"https://unauthorized.invalid/",allowedScopes:[],surfaces:["shell"],status:"active"}}); assert.equal(catalogWrite.status,403,"ordinary app identities cannot publish catalog entries"); const installed=e.api.handle({ method:"POST", path:"/v1/apps/space-mountain/install", headers:{ authorization:`Bearer ${e.token}`, "x-spmt-tenant":"tenant-a" }, body:{} }); assert.equal(installed.status,200); assert.equal(e.control.listInstalls("tenant-a")[0].appId,"space-mountain"); e.control.setEntitlement({ tenantId:"tenant-a", appId:"space-mountain", key:"premium", value:true }); const ent=e.api.handle({ method:"GET", path:"/v1/entitlements?appId=space-mountain", headers:{ authorization:`Bearer ${e.token}`, "x-spmt-tenant":"tenant-a" } }); assert.equal(ent.status,200); assert.equal(ent.body[0].value,true); const denied=e.api.handle({ method:"GET", path:"/v1/apps/installs", headers:{ authorization:`Bearer ${e.token}`, "x-spmt-tenant":"tenant-b" } }); assert.equal(denied.status,403); } finally { cleanup(e); } });

test("overlay widgets and runtime health are durable self-scoped contracts across SDK, CLI, API and MCP", async () => { const e=env(); try { e.control.installApp("tenant-a","space-mountain"); e.auth.registerServiceIdentity({serviceId:"space-mountain",credential:"space-mountain-overlay-secret-12345",scopes:["overlay:widgets:read","overlay:widgets:write","runtime:read","runtime:write"],tenantMode:"allow-list",tenantIds:["tenant-a"]}); const accessToken=e.auth.issueServiceAccess("space-mountain","space-mountain-overlay-secret-12345").accessToken; const fetchImpl=async(url,init={})=>{const parsed=new URL(String(url));const headers=Object.fromEntries(new Headers(init.headers).entries());let body;if(typeof init.body==="string"&&init.body)body=JSON.parse(init.body);const response=e.api.handle({method:init.method??"GET",path:`${parsed.pathname}${parsed.search}`,headers,...(body===undefined?{}:{body})});return new Response(response.body===undefined?null:JSON.stringify(response.body),{status:response.status,headers:{"content-type":"application/json"}});}; const client=new SpmtClient({baseUrl:"https://green.spmt.invalid",appId:"space-mountain",getAccessToken:()=>accessToken,fetchImpl}); const manifest={schemaVersion:1,appId:"space-mountain",widgetId:"command-status",title:"Command status",kind:"status",rendererUrl:"https://spacemountain.live/overlay/command-status",requiredScopes:["workspace:read"],supportsAudio:false,supportsInteraction:false}; const registered=await client.registerOverlayWidget("tenant-a",manifest); assert.equal(registered.manifest.widgetId,"command-status"); assert.equal((await runSpmtCli(["overlay","list","tenant-a"],client))[0].manifest.appId,"space-mountain"); const mcp=new SpmtMcpServer(e.operations); const reported=mcp.handle({jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"spmt.runtime.state.report",arguments:{tenantId:"tenant-a",state:"ready",detail:"provider streams connected"}}},{accessToken,protocolVersion:SPMT_MCP_PROTOCOL_VERSION}); assert.equal(reported.result.structuredContent.state,"ready"); const runtime=e.api.handle({method:"GET",path:"/v1/runtime/state",headers:{authorization:`Bearer ${accessToken}`,"x-spmt-tenant":"tenant-a"}}); assert.equal(runtime.status,200); assert.equal(runtime.body[0].appId,"space-mountain"); assert.equal(e.store.listOverlayWidgets("tenant-a","space-mountain").length,1); assert.equal(e.store.listRuntimeProjections("tenant-a","space-mountain")[0].detail,"provider streams connected"); const spoofed=e.api.handle({method:"PUT",path:"/v1/overlay/widgets",headers:{authorization:`Bearer ${accessToken}`,"x-spmt-tenant":"tenant-a"},body:{...manifest,appId:"another-app"}}); assert.equal(spoofed.status,403); const wrongTenant=e.api.handle({method:"POST",path:"/v1/runtime/state",headers:{authorization:`Bearer ${accessToken}`,"x-spmt-tenant":"tenant-b"},body:{state:"ready"}}); assert.equal(wrongTenant.status,403); assert.ok(e.store.listAudit("tenant-a").some((item)=>item.action==="overlay.widgets.register")); assert.ok(e.store.listAudit("tenant-a").some((item)=>item.action==="runtime.state.report")); } finally { cleanup(e); } });

test("platform event and outbox are atomic, leased, retried and completed", async () => { const e=env(); try { const published=e.operations.execute({ name:"events.publish", input:{ tenantId:"tenant-a", type:"workspace.changed", payload:{ revision:2 }, idempotencyKey:"evt-1" } }, { accessToken:e.token }); assert.equal(published.result.duplicate,false); const visible=e.operations.execute({ name:"events.list", input:{ tenantId:"tenant-a",limit:10,type:"workspace.changed" } }, { accessToken:e.token }); assert.equal(visible.result.length,1); assert.equal(visible.result[0].idempotencyKey,"evt-1"); assert.equal(e.authority.listOutbox().length,1); let calls=0; const dispatcher=new OutboxDispatcher({ authority:e.authority, workerId:"worker-1", retrySeconds:1, maxAttempts:3, deliver:async()=>{ calls++; if(calls===1) throw new Error("temporary"); } }); const first=await dispatcher.runOnce(); assert.equal(first.retried,1); assert.equal(e.authority.listOutbox()[0].state,"pending"); e.advance(2); const second=await dispatcher.runOnce(); assert.equal(second.delivered,1); assert.equal(e.authority.listOutbox()[0].state,"delivered"); const duplicate=e.operations.execute({ name:"events.publish", input:{ tenantId:"tenant-a", type:"workspace.changed", payload:{ revision:2 }, idempotencyKey:"evt-1" } }, { accessToken:e.token }); assert.equal(duplicate.result.duplicate,true); assert.equal(e.authority.listOutbox().length,1); } finally { cleanup(e); } });

test("a signed-in tenant member can publish only a validated Simulation Room preview without a second token", () => { const e=env(); try { const token=e.auth.issueHumanSession({userId:"owner-1",scopes:["events:read"],tenantIds:["tenant-a"]}).accessToken,payload={schemaVersion:1,roomId:"overlay:public:scene-1",lane:"overlay",direction:"preview",title:"Overlay preview",body:"One visible source",data:{sceneId:"scene-1"},occurredAt:"2026-09-05T12:00:00.000Z"}; const published=e.api.handle({method:"POST",path:"/v1/simulation-rooms/events",headers:{authorization:`Bearer ${token}`,"x-spmt-tenant":"tenant-a","idempotency-key":"simulation-1"},body:payload}); assert.equal(published.status,200); assert.equal(published.body.event.type,"spmt.simulation-room.event.v1"); assert.equal(published.body.event.sourceAppId,"owner-1"); const arbitrary=e.api.handle({method:"POST",path:"/v1/events",headers:{authorization:`Bearer ${token}`,"x-spmt-tenant":"tenant-a","idempotency-key":"arbitrary-1"},body:{type:"workspace.changed",payload:{revision:99}}}); assert.equal(arbitrary.status,403); const unsafe=e.api.handle({method:"POST",path:"/v1/simulation-rooms/events",headers:{authorization:`Bearer ${token}`,"x-spmt-tenant":"tenant-a","idempotency-key":"simulation-unsafe"},body:{...payload,data:{access_token:"must-not-leak"}}}); assert.equal(unsafe.status,400); } finally { cleanup(e); } });

test("control-plane state survives SQLite reopen", () => { const e=env(); e.control.installApp("tenant-a","space-mountain"); e.control.setEntitlement({ tenantId:"tenant-a", appId:"space-mountain", key:"tier", value:"pro" }); e.control.registerOverlayWidget({tenantId:"tenant-a",manifest:{schemaVersion:1,appId:"space-mountain",widgetId:"health",title:"Health",kind:"status",rendererUrl:"https://spacemountain.live/overlay/health",requiredScopes:["workspace:read"],supportsAudio:false,supportsInteraction:false}}); e.control.reportRuntimeState({tenantId:"tenant-a",appId:"space-mountain",state:"ready"}); e.store.close(); const reopened=new SqliteAuthorityStore(e.path); try { const control=new ControlService({ store:reopened }); assert.equal(control.getTenant("tenant-a").ownerUserId,"owner-1"); assert.equal(control.getApp("space-mountain").name,"SpaceMountain"); assert.equal(control.listInstalls("tenant-a")[0].enabled,true); assert.equal(control.listEntitlements("tenant-a","space-mountain")[0].value,"pro"); assert.equal(control.listOverlayWidgets("tenant-a")[0].manifest.widgetId,"health"); assert.equal(control.listRuntimeProjections("tenant-a")[0].state,"ready"); } finally { reopened.close(); rmSync(e.dir,{recursive:true,force:true}); } });
