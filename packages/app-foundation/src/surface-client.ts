import { assertAppSurfaceManifestV1, type AppSurfaceManifestV1 } from "@spmt/contracts/surface";

export function appSurfaceBrowserJs(manifest: AppSurfaceManifestV1) {
  const value = JSON.stringify(assertAppSurfaceManifestV1(manifest)).replace(/</g, "\\u003c");
  return String.raw`;(()=>{const manifest=${value},body=document.body,html=document.documentElement;let hostOrigin='*';const style=document.createElement('style');style.dataset.spmtSurfaceClient='1';style.textContent='html,body{overscroll-behavior:none}html[data-spmt-surface-mode="shell"],html[data-spmt-surface-mode="shell"] body,.owned[data-surface="shell"]{background:transparent!important;background-color:transparent!important;background-image:none!important;color-scheme:normal!important}.owned[data-surface="shell"]>.spmt-product-backdrop{display:none!important}.owned[data-surface="shell"] main{height:var(--spmt-shell-available-height,100dvh)!important;min-height:0!important;grid-template-columns:minmax(0,1fr)!important;padding:clamp(8px,1.4vw,18px)!important;overflow:hidden!important}.owned[data-surface="shell"] .tabs{display:none!important}.owned .home{height:100%!important;min-height:0!important;overflow:hidden!important}.owned .hero{max-height:100%!important;overflow:hidden!important}.owned .app-page{height:100%!important;min-height:0!important;overflow:auto!important;overscroll-behavior:contain!important;scrollbar-gutter:stable}.owned *{scrollbar-width:thin;scrollbar-color:transparent transparent}.owned *:hover{scrollbar-color:color-mix(in srgb,var(--spmt-accent) 62%,transparent) transparent}.owned *::-webkit-scrollbar{width:4px;height:4px}.owned *::-webkit-scrollbar-track{background:transparent}.owned *::-webkit-scrollbar-thumb{border-radius:99px;background:transparent}.owned *:hover::-webkit-scrollbar-thumb{background:color-mix(in srgb,var(--spmt-accent) 62%,transparent)}.owned[data-surface="shell"] .hero h1{font-size:clamp(40px,8vw,92px)!important}.owned[data-surface="shell"] .hero{gap:clamp(10px,2vh,18px)!important}.owned[data-surface="shell"] .actions{margin:0!important}@media(max-width:560px){.owned[data-surface="shell"] main{padding:8px!important}.owned[data-surface="shell"] .hero{gap:10px!important}.owned[data-surface="shell"] .hero h1{font-size:clamp(38px,15vw,66px)!important}}';document.head.append(style);function setMode(mode){html.dataset.spmtSurfaceMode=mode;if(mode==='shell'){html.style.setProperty('background','transparent','important');html.style.setProperty('background-color','transparent','important');html.style.setProperty('color-scheme','normal','important');body.style.setProperty('background','transparent','important');body.style.setProperty('background-color','transparent','important');body.style.setProperty('color-scheme','normal','important')}}function send(message){if(window.parent!==window)window.parent.postMessage(message,hostOrigin)}function show(pageId){const page=[...document.querySelectorAll('[data-page],[data-hmo-view-panel]')].find(node=>(node.getAttribute('data-page')||node.getAttribute('data-hmo-view-panel'))===pageId);if(!page)return false;for(const node of document.querySelectorAll('[data-page],[data-hmo-view-panel]'))node.hidden=node!==page;for(const button of document.querySelectorAll('[data-nav]'))button.setAttribute('aria-current',button.getAttribute('data-nav')===pageId?'page':'false');body.dataset.spmtPage=pageId;if(body.dataset.hmoView!==undefined)body.dataset.hmoView=pageId;send({protocol:'spmt.surface',version:1,type:'page.changed',appId:manifest.appId,pageId});return true}function publish(){send({protocol:'spmt.surface',version:1,type:'surface.manifest',manifest})}function addShortcuts(){const actions=document.querySelector('.actions');if(!actions||!Array.isArray(manifest.shortcuts))return;for(const shortcut of manifest.shortcuts){if(actions.querySelector('[data-spmt-surface-shortcut="'+CSS.escape(shortcut.id)+'"]'))continue;const button=document.createElement('button');button.type='button';button.className='button';button.dataset.spmtSurfaceShortcut=shortcut.id;button.textContent=shortcut.label;button.addEventListener('click',()=>show(shortcut.pageId));actions.append(button)}}window.addEventListener('message',event=>{const message=event.data;if(message?.protocol==='spmt.embed'&&message?.version===1&&message?.type==='host.hello'&&message.launch?.appId===manifest.appId){hostOrigin=event.origin||hostOrigin;setMode(message.launch.surfaceMode||'standalone');publish();return}if(!message||message.protocol!=='spmt.surface'||message.version!==1||message.appId!==manifest.appId)return;if(hostOrigin!=='*'&&event.origin!==hostOrigin)return;if(message.type==='page.open')show(message.pageId)});for(const button of document.querySelectorAll('[data-nav]'))button.addEventListener('click',()=>{const pageId=button.getAttribute('data-nav');if(pageId)send({protocol:'spmt.surface',version:1,type:'page.changed',appId:manifest.appId,pageId})});addShortcuts();publish()})();`;
}

export function productSurfaceManifest(input: {
  appId: string;
  sceneUrl: string;
  scenePosition?: string;
  sections: ReadonlyArray<{ id: string; label:string; body?: string; glyph?: string }>;
  shortcuts?: ReadonlyArray<{ id: string; label: string; pageId: string }>;
}): AppSurfaceManifestV1 {
  return assertAppSurfaceManifestV1({
    schemaVersion: 1,
    appId: input.appId,
    scene: { imageUrl: input.sceneUrl, ...(input.scenePosition ? { imagePosition: input.scenePosition } : {}) },
    pages: [
      { id: "home", label: "Home", description: "Application home", glyph: "⌂", home: true },
      ...input.sections.map((section) => ({ id: section.id, label: section.label, ...(section.body ? { description: section.body } : {}), ...(section.glyph ? { glyph: section.glyph } : {}) })),
    ],
    ...(input.shortcuts?.length ? { shortcuts: input.shortcuts.map((shortcut) => ({ ...shortcut })) } : {}),
  });
}
